<% @products.each do |product| %>
  <h2><a href="/client/products/<%= product["id"] %>"><%= product["name"] %></a>
  </h2>
  <h3>Price <% product["price"] %></h3>
  <h5>Description <%= product["description"] %></h5>
  
<% end %>